{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Arvolo main window.",
  "windows": ["main"],
  "permissions": [
    "autostart:default",
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:default",
    "notification:default",
    "opener:default"
  ]
}
